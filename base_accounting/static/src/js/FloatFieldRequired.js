odoo.define('web.FloatFieldRequired', function (require) {
    "use strict";

        //  Fix mandatory float field
    // var Widgets = require('web.form_widgets');
    // var FloatField = Widgets.FieldFloat;
    // FloatField.include({
    //     is_false: function () {
    //         return this.get('value') === false || this.get('value') === 0.0;
    //     }
    // });

        // disable create and edit in M2O field
    // var FormRelational = require('web.form_relational');
    // var FieldMany2One = FormRelational.FieldMany2One;
    // FieldMany2One.include({
    //     init: function (field_manager, node) {
    //         this._super(field_manager, node);
    //         this.can_create = false;
    //     }
    // });

});
